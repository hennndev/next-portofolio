import React from 'react'
import Head from 'next/head'
import { useRouter } from 'next/router'
import { HomeIcon } from '@heroicons/react/outline'

const NotFound = () => {

    const router = useRouter()

    return (

        <>
            <Head>
                <title>Page Not Found</title>
                <meta name="description" content="Generated by create next app" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <div className="min-h-screen bg-gray-100 text-gray-700 dark:bg-gray-900 dark:text-gray-200 py-10 px-5 transition duration-200 ease-in">
                <div className="flex items-center justify-center flex-col mt-40">
                    <h1 className="text-3xl mb-5">Ooops, Page Not Found</h1>
                    <button className="bg-gray-300 dark:bg-gray-200 dark:text-gray-700 py-3 px-6 rounded flex items-center font-medium" onClick={() => router.push('/')}>
                        <HomeIcon className="h-6 mr-2"/>
                        Back to Homepage
                    </button>
                </div>
            </div>
        </>
    )
}

export default NotFound
